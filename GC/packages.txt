# packages.txt — system apt packages for Streamlit Cloud / HuggingFace Spaces
#
# This file is used by Streamlit Cloud and HuggingFace Spaces (sdk: docker
# falls back to this for apt installs in some build environments).
#
# For Google Cloud Run these packages are installed directly in the Dockerfile
# RUN apt-get install layer — this file is kept for cross-platform compatibility.
openbabel
libcairo2-dev
libpango1.0-dev
pkg-config
python3-dev
libgirepository1.0-dev
